// var event_logic = () => {
//   let event_elements = document.getElementById("event_elements");
//   event_elements.innerHTML = ("<%= escape_javascript (render :partial => 'events_list', locals: {events: @events, markers: @markers}) %>")
// };

// event_logic();


// var index = client.initIndex('Event');
// index.search(@search_params, { hitsPerPage: 10, page: 0 })
//   .then(function searchDone(content) {
//     console.log(content)
//   })
//   .catch(function searchFailure(err) {
//     console.error(err);
//   });
