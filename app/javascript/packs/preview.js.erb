// $("#modal-window").find(".modal-content").html("<%= j (render 'preview') %>");
// $("#modal-window").modal();

$("#modal-window").html("<%= escape_javascript(render 'project/new_release') %>");
// attempt to open pop-up : must make ticket ///
